# liquidation_regime, funding black window, crowding. Veto only, never an entry reason.
# Numbers and flags come from the Rust context snapshot.

WIRED = True
SOURCE = "rust_closed_1m"
ENTRY_REASON = False
