# SOL 腳本 F：美盤真 L2（不是 live）

日期：**2026-09-16 13:18 → 20:44 UTC**  
過夜窗（極薄→亞盤 549 根）對上足跡的牆只有 2 根。本窗是合完 #17 之後，用同一支公共 WS 捕獲往前錄**美盤**。不是選 300 vs 400，不是 live。

路徑：`scripts/capture_okx_l2.py`（trades + incremental `books`）→ `/tmp/sol_l2_us/` JSONL → `orderflowd --replay --book-replay` → 凍結 `book_closed` → `scripts/oos_scripts_from_journal.py`。不准用成交量堆腦補牆。不准改 `wall_mult`。

---

## 覆蓋

| | |
|---|---|
| 窗 | **2026-09-16 13:18 → 20:44 UTC**（美盤 UTC 13–21；forming 20:45 不計） |
| 成交 | **137947** 筆 OKX `SOL-USDT-SWAP` 公共 WS |
| books | **260472** 幀；**2** 次 snapshot（開連 1 + 重接 1）；檔內 seq 缺口 **0** |
| 已收盤 1m | **441**（全是美盤）；`book_closed` **441**；`okx_book_ok=true` |
| 品質 | 鐘上缺 **6** 分鐘（`gap_minutes=6`）；分鐘跳空 15:16→15:23；`late_trade=0`；`copied_price_onto_okx=false`；checksum 失敗 **0** |
| 結束原因 | 20:45 UTC `hard stale 40s` `os._exit`。沒錄到原定 21:00 |

JSONL 留 `/tmp`。這 441 根仍**不夠**凍結 F：對上足跡的牆有 **13** 根，比亞盤密，但真吃牆仍是 0。

---

## 缺口（誠實）

不是完整 13:00–21:00。13:18 才開始。20:45 被捕獲腳本硬退出，少最後約 15 分鐘。

15:14 前後資料卡住；看門狗在 15:23 因 stale 411 秒殺掉舊行程並重接。鐘跳 15:16→15:23（缺 6 分鐘）。重接再要一張 snapshot，牆計數重設。`rebuilds=2` 對得上 2 張 snapshot。檔內 seq 缺口 0 只表示**每一段連線內部**連續。缺的那 6 分鐘不當成「沒有牆」。

之後這段（15:23→20:45）連續約 5 小時。VM 後來閒置凍結，回放拖到之後才做。資料檔停在 20:45，沒有被後來的看門狗改寫。

---

## F 四種對讀（引擎已閉合棒）

全書健康：**441 / 441 `book_ok`**。`script_f=computed`。

畫面把沒有 3×中位牆的 `not_evaluated` 算進「無牆 / 不算」（100 根）。沒有改分類器。

| | 任意 3×中位牆 | 牆在當根 POC 或堆疊上 |
|---|---|---|
| 根數 | 441 | **13** |
| 真吃牆 | 0 | 0 |
| 讓路 | 1 | 0 |
| 吸收 | 43 | **6** |
| 假牆 | 297 | **4** |
| 無牆 / 不算 | 100 | **3** |

任意大牆仍是假牆居多（297/441）。那是做市減補噪音，**不是 F 的主詞。**

派內是**牆與足跡對讀**。對上 POC 的 **1** 根；對上堆疊的 **13** 根（POC 那根也在堆疊上）。13 根裡：吸收 6、假牆 4、不算 3。真吃牆 0。讓路出現 1 次（16:55，**不在** POC/堆疊上）。

讓路仍是否決。F **不當進場確認**。不准因為美盤對上足跡的牆比較多就改 `wall_mult`、不准把成交量堆當成牆。

過夜亞盤 549 根對上 2 根。這窗美盤 441 根對上 **13** 根。密度比較高，仍不夠凍結 F。

---

## 這窗不當什麼

- 不當 300 vs 400 的樣本（武裝堆疊 300=54 / 400=33；A 打穿 300=6/34、400=3/22，Wilson 仍重疊）
- 不當 `out_of_sample_validated`
- 不當 live
- 不當「美盤對上 13 根所以 F 能開倉」
- 不當「完整 8 小時美盤」——晚開、早收、中間缺 6 分鐘

定義不改。下一步若還要 F，是再錄完整美盤，不是改牆倍數。

禁止項自檢：沒有腦補牆；沒有改 `wall_mult`；沒有開 live；沒有選邊；JSONL 不進 git。
