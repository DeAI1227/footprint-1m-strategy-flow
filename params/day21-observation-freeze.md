# 第 21 天：21 日觀察稿（仍禁止 live）

日期：**2026-09-21**（時程第 21 天）  
壓縮計算：2026-09-03。這不是樣本外驗證，不是實盤授權。

---

## SOL（主樣本 / OKX 執行足跡）

| 鍵 | 凍結假設 | 仍開放 |
|---|---|---|
| 桶 | **0.01** | 活躍時段 200%+p25 的 3 檔接近每根才試 0.02。不要試 0.10 |
| 最小量 | **時段非空單側 p25，雙邊** | 絕對 SOL 數不凍結 |
| 記錄 | **200%** | 只顯示 |
| 武裝 | **300%（Dale）∥ 400%（Valtos）** | 兩週 + SUI 對照後仍分不開。不准 350% |
| 堆疊 | **3**；棒向 On | 4 檔只當更嚴備註 |
| `SWING_N` | **5** | — |
| A `LEAVE_BARS` | **1** | 離開後打穿約 19% |
| C `TRAP_BARS` | **3** | 3 根內收回 45% |
| 未完成 / G | 顯示；**不開倉** | 關鍵位仍多半延伸 |
| F | **`not_evaluated`** | 無 L2 |
| 清算否決 | 1H OI ≤ **−2%** 的那一小時；或當根強平名義 ≥ **該批 p95** | 絕對 USDT 不凍結 |
| 資金費黑窗 | **00/08/16 UTC ±15m** 不開新倉 | 不是把 % 加嚴 |
| 共振 | 計算方向；模式 **`off`** | 三所全同向約 54% |

週 2 句子層失效畫面維持 `params/day14-sol-week2-freeze.md`。本週沒有重開眼睛。

## SUI（驗證樣本 / 另表）

| 鍵 | 凍結假設 | 與 SOL 的差異 |
|---|---|---|
| 桶 | **0.0001**（原生 tick） | **不能抄 0.01**（0.01 跨 3 列 = 0%） |
| 最小量 | 時段非空單側 p25，雙邊 | 快照亞≈319 / 歐≈277 / 美≈228 / 極薄≈198 **SUI** |
| 記錄 / 武裝 | 200 記錄；**300∥400 並列** | 密度與 SOL 同量級（300% 3 檔約 11%），數字仍分表 |
| 堆疊 / 棒向 | 3 / On | 能出現可數 3 檔 |
| A | 同一句子，`LEAVE_BARS=1` | 打穿約 **23%**（美盤 29%），略漏於 SOL。不回頭放寬 SOL |
| `shadow_only`？ | **否**（語言可跑） | **live 仍關** |
| 空桶 | 這段忙時段空列中位 0% | 斜對角零對約每根 6.4（SOL 3.3）。Ignore Zero On |

## 共同（兩張表都抄）

- 斜對角、Ignore Zero On、當根 70% VA、未完成不當進場  
- 只對已收盤 1m 開倉決策；forming 只撤單 / 風控  
- 三所成交量不加總；不看 A 打 B  
- 不算 Market Profile、VWAP、Naked POC、Kill Zone、布林 / 肯特納  
- F 無 L2 就 `not_evaluated`  
- **參數未在樣本外驗證前禁止 live**

## 21 天要回答的五件事

1. SOL 活躍時段 300%+p25 的 3 檔大約每 9–10 分鐘一顆（美盤更密），不是每根都有。  
2. 300 與 400 **必須並列**。  
3. SOL 離開後回踩：拒絕 48%、打穿 19%；約 32% 武裝堆疊根本沒離開。  
4. SUI 不能抄 SOL 桶寬：0.01 在 SUI 1m 裝不滿 3 列。SUI 用 0.0001。  
5. 整段作廢：1H OI ≤ −2% 的小時、強平分鐘 p95、資金費 ±15m、壞資料 / 缺所、極薄仍用自己的 p25 而不是關語言。

未完成 = 還在用勝率、主力、鐵板、VWAP、整數關選數字。本稿沒有那些。
