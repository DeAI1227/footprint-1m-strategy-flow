# SOL 腳本 F：過夜窗真 L2（極薄→亞盤，不是 live）

日期：**2026-09-15 22:50 → 09-16 07:58 UTC**  
上一窗（09-13→09-14，169 根、缺 70 分鐘）對上足跡的牆只有 3 根，不夠凍結 F。本窗是合完 #15 之後，用同一支公共 WS 捕獲往前錄到亞盤收場。目標是**比較乾淨的 books**（少缺口），不是選 300 vs 400，不是 live。

路徑：`scripts/capture_okx_l2.py`（trades + incremental `books`）→ `/tmp/sol_l2_overnight/` JSONL → `orderflowd --replay --book-replay` → 凍結 `book_closed` → `scripts/oos_scripts_from_journal.py`。不准用成交量堆腦補牆。不准改 `wall_mult` 讓假牆好看。

---

## 覆蓋

| | |
|---|---|
| 窗 | **2026-09-15 22:50 → 09-16 07:58 UTC**（極薄切入亞盤；forming 07:59 不計） |
| 成交 | **88452** 筆 OKX `SOL-USDT-SWAP` 公共 WS |
| books | **320193** 幀；**5** 次 snapshot（開連 1 + 重接 4）；檔內 seq 缺口 **0** |
| 已收盤 1m | **549**（極薄 70 / 亞 479）；`book_closed` **549**；`okx_book_ok=true` |
| 品質 | 鐘上連續（`gap_minutes=0`）；分鐘跳空 **0**；`late_trade=0`；`copied_price_onto_okx=false`；checksum 失敗 **0** |
| 結束原因 | 看門狗到 **08:00 UTC** 正常收工（`WATCH_EXIT=0`）。行程沒被 `pkill` |

JSONL 留 `/tmp`。這 549 根仍**不夠**凍結 F：對上足跡的牆只有 **2** 根，而且仍是極薄+亞盤，沒有美盤。

---

## 重接（誠實）

不是「整晚 WS 一次都沒斷」。公共連線自己掉了 **4** 次，每次約 1–2 分鐘就回來；行程沒死、看門狗沒重拉：

- ~01:27 掉 → 01:28:33 重連
- ~02:15 掉 → 02:16:58 重連
- ~04:28 掉 → 04:28:58 重連
- ~05:00 掉 → 05:01:10 重連

重接會再要一張 snapshot。引擎重建、重設牆計數。`rebuilds=5` 對得上 5 張 snapshot。檔內 seq 缺口 0 只表示**每一段連線內部**連續。那 1–2 分鐘裡可能少幀，但鐘沒跳過任何已收盤分鐘，所以 `gap_minutes=0`。不當成「這幾分鐘確定沒有牆」。

上一窗缺 70 分鐘。本窗鐘是連續的。仍不是美盤。

---

## F 四種對讀（引擎已閉合棒）

全書健康：**549 / 549 `book_ok`**。`script_f=computed`。

畫面把沒有 3×中位牆的 `not_evaluated` 算進「無牆 / 不算」（114 根）。沒有改分類器。

| | 任意 3×中位牆 | 牆在當根 POC 或堆疊上 |
|---|---|---|
| 根數 | 549 | **2** |
| 真吃牆 | 0 | 0 |
| 讓路 | 0 | 0 |
| 吸收 | 37 | **2** |
| 假牆 | 398 | **0** |
| 無牆 / 不算 | 114 | **0** |

任意大牆仍是假牆居多（398/549）。那是做市減補噪音，**不是 F 的主詞。**

派內是**牆與足跡對讀**。對上 POC 的 **1** 根；對上堆疊的 **2** 根（02:17 吸收，bid 97.00，POC+堆疊；02:20 吸收，bid 97.10，只堆疊）。真吃牆與讓路這窗都沒出現。

02:17 是第二次重接（02:16:58）後的第一根已收盤，牆計數剛重設，不當成乾淨的對讀樣本。02:20 隔了幾分鐘。兩根都在亞盤。

上一窗 169 根（有缺口）對上足跡 3 根。這窗 549 根、鐘連續，對上足跡 **2** 根。樣本變長、缺口變少，對上足跡的牆沒變密。不夠凍結 F。

讓路仍是否決（本窗 0 次）。F **不當進場確認**。不准因為假牆很多就改 `wall_mult`、不准把成交量堆當成牆。

---

## 這窗不當什麼

- 不當 300 vs 400 的樣本（武裝堆疊 300=20 / 400=12；A 打穿 300=2/14、400=0/7，Wilson 仍什麼都分不出）
- 不當 `out_of_sample_validated`
- 不當 live
- 不當「假牆 72% 所以 F 能賺」
- 不當「已有美盤健康 L2」——本窗極薄+亞盤
- 不當「549 根連續就等於 F 可凍結」——對上足跡的牆仍只有 2 根

上一窗證明拉長之後牆仍稀。這窗證明**鐘連續之後牆仍稀**。定義不改。下一步若還要 F，需要美盤或其他時段，不是再把同一段亞盤加長。

禁止項自檢：沒有腦補牆；沒有改 `wall_mult`；沒有開 live；沒有選邊；JSONL 不進 git。
