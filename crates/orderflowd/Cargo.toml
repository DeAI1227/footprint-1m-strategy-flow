[package]
name = "orderflowd"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Process: load params, boot shadow/sim, refuse live; 1m footprint + L2 + context replay."

[dependencies]
orderflow-domain.workspace = true
orderflow-ingest = { path = "../orderflow-ingest" }
orderflow-footprint = { path = "../orderflow-footprint" }
orderflow-book = { path = "../orderflow-book" }
orderflow-context = { path = "../orderflow-context" }
orderflow-exec = { path = "../orderflow-exec" }
orderflow-ops = { path = "../orderflow-ops" }
orderflow-calibrate = { path = "../orderflow-calibrate" }
serde_json.workspace = true
tokio.workspace = true
