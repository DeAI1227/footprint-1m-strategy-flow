[package]
name = "orderflowd"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Process: load params, boot shadow/sim, refuse live; OKX 1m replay."

[dependencies]
orderflow-domain.workspace = true
orderflow-clock = { path = "../orderflow-clock" }
orderflow-ingest = { path = "../orderflow-ingest" }
orderflow-exec = { path = "../orderflow-exec" }
serde_json.workspace = true
tokio.workspace = true
