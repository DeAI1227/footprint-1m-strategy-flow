[package]
name = "orderflow-footprint"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Per-venue 1m footprint matrix. No VWAP/TPO. Python must not build a second matrix."

[dependencies]
orderflow-domain.workspace = true
orderflow-clock = { path = "../orderflow-clock" }
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
orderflow-ingest = { path = "../orderflow-ingest" }
tempfile = "3"
